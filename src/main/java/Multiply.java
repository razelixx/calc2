public class Multiply extends Summa{
    protected int multyply(int firstNum, int secondNum) {
        int result = firstNum * secondNum;
        System.out.print(result);
        return result;
    }
}
